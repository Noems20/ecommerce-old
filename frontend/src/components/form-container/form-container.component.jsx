import React from 'react';

// Styles
import { Container } from './form-container.styles';

const SignCardContainer = ({ children }) => {
  return <Container>{children}</Container>;
};

export default SignCardContainer;
